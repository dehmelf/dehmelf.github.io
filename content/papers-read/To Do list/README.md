### What this is
`index.html` is a **self-contained HTML/CSS mock** that matches the look of the screenshot: dark framed UI, left file list, right code panel, and the small `1/137` badge.

### How to view it
- Open `index.html` in your browser (double-click it in Finder, or drag it into a browser window).

### Where to replace text later
- **Left panel (file list)**: edit the `<ul class="file-list"> ... </ul>` contents.
- **Bottom status line**: edit the `<div class="status"> ... </div>` contents.
- **Right panel badge**: edit the `<div class="badge"> ... </div>` contents.
- **Code area**: edit the lines inside `<pre><code> ... </code></pre>`.

If you send me the exact text you want in each area, I can drop it in while preserving the styling.


